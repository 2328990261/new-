# 教师验证码表 - 手动修复指南

## 问题
登录时报错：`Table 'myjiajiao.fa_teacher_verification_codes' doesn't exist`

## 快速修复方法

### 方法1：使用批处理脚本（最简单）

1. 双击运行：`quick_fix.bat`
2. 等待执行完成
3. 刷新浏览器重试登录

### 方法2：使用phpMyAdmin（推荐）

1. 打开phpMyAdmin
2. 选择 `myjiajiao` 数据库
3. 点击"SQL"标签
4. 复制下面的SQL代码并执行：

```sql
CREATE TABLE IF NOT EXISTS `fa_teacher_verification_codes` (
  `id` int(11) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `teacher_id` int(11) DEFAULT NULL COMMENT '教师ID（登录时记录）',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱地址',
  `code` varchar(20) NOT NULL COMMENT '验证码',
  `type` varchar(20) DEFAULT 'email' COMMENT '验证码类型：email-邮箱验证，login-登录记录，reset-密码重置',
  `is_used` tinyint(1) DEFAULT 0 COMMENT '是否已使用：0-未使用，1-已使用',
  `expire_time` int(11) DEFAULT NULL COMMENT '过期时间（Unix时间戳）',
  `create_time` int(11) DEFAULT NULL COMMENT '创建时间（Unix时间戳）',
  `update_time` int(11) DEFAULT NULL COMMENT '更新时间（Unix时间戳）',
  PRIMARY KEY (`id`),
  KEY `idx_email` (`email`),
  KEY `idx_code` (`code`),
  KEY `idx_teacher_id` (`teacher_id`),
  KEY `idx_create_time` (`create_time`),
  KEY `idx_expire_time` (`expire_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师验证码表';
```

5. 点击"执行"
6. 看到"查询成功"提示即可

### 方法3：使用MySQL命令行

1. 打开命令提示符（CMD）
2. 登录MySQL：
   ```cmd
   mysql -h127.0.0.1 -P3309 -ujiajiao -p123456 myjiajiao
   ```
3. 执行SQL：
   ```sql
   source C:/path/to/quick_fix_verification_table.sql
   ```
   或者直接复制粘贴上面方法2中的SQL代码

4. 输入 `exit` 退出MySQL

### 方法4：使用Navicat等数据库工具

1. 打开Navicat
2. 连接到 myjiajiao 数据库
3. 右键数据库 → 执行SQL文件
4. 选择 `quick_fix_verification_table.sql`
5. 执行

## 验证是否成功

执行以下SQL查询：

```sql
SHOW TABLES LIKE 'fa_teacher_verification_codes';
```

如果返回结果显示表名，说明创建成功！

## 数据库连接信息

根据 `.env` 文件配置：
- 主机：127.0.0.1
- 端口：3309
- 数据库：myjiajiao
- 用户名：jiajiao
- 密码：123456

## 注意事项

1. **表名前缀**：此项目使用 `fa_` 作为表前缀
2. **时间戳格式**：使用Unix时间戳（整数），不是datetime格式
3. **字符集**：utf8mb4，支持emoji等特殊字符

## 常见问题

### Q: 执行SQL时提示"表已存在"
A: 这是正常的，说明表已经创建过了。可以忽略此提示。

### Q: 提示"权限不足"
A: 确保数据库用户有CREATE TABLE权限。可以联系数据库管理员。

### Q: 找不到MySQL命令
A: MySQL可能未添加到系统PATH。使用phpMyAdmin或其他图形化工具代替。

## 修复后测试

1. 刷新浏览器
2. 访问教师登录页面：http://localhost:3001/teacher-login
3. 尝试注册或登录
4. 应该不再报错

## 需要帮助？

如果以上方法都无法解决，请检查：
1. 数据库服务是否正常运行
2. 数据库连接信息是否正确
3. 数据库用户是否有足够权限
