# Git 上传前检查清单

## 📋 必须检查项

### 1. 敏感文件保护 ✓

- [x] `.gitignore` 文件已创建
- [ ] 检查 `.env` 文件是否包含敏感信息
- [ ] 检查证书文件（.pem）是否被排除
- [ ] 检查数据库配置文件

**验证命令**：
```bash
git status
```

确保以下文件显示为 "Untracked" 或不在列表中：
- `backend/.env`
- `backend/cert/wechat/*.pem`
- `backend/runtime/`

### 2. 依赖目录排除 ✓

- [x] `node_modules/` 已排除
- [x] `backend/vendor/` 已排除
- [x] `backend/runtime/` 已排除

### 3. 文档完整性 ✓

- [x] `README.md` 已创建且内容完整
- [x] 包含项目简介
- [x] 包含安装步骤
- [x] 包含使用说明
- [x] 包含常见问题

### 4. 代码质量检查

- [ ] 删除所有测试文件（test_*.php）
- [ ] 删除所有调试代码（console.log, var_dump）
- [ ] 删除所有临时文件（*.tmp, *.bak）

### 5. 配置文件检查

- [ ] 确认 `.example.env` 文件存在（作为配置模板）
- [ ] 确认所有配置项都有说明
- [ ] 确认没有硬编码的密码或密钥

## 🔍 详细检查步骤

### 步骤 1: 检查敏感文件

```bash
cd e:\123.207.52.46_8088\new_system

# 检查 .env 文件
type backend\.env

# 如果包含真实密码，确保它在 .gitignore 中
```

### 步骤 2: 检查 Git 状态

```bash
git status
```

**应该看到**：
- ✅ 源代码文件（.php, .vue, .js）
- ✅ 配置模板（.example.env）
- ✅ 文档文件（.md）
- ✅ 数据库脚本（.sql）

**不应该看到**：
- ❌ .env 文件
- ❌ node_modules/
- ❌ vendor/
- ❌ runtime/
- ❌ *.pem 证书文件
- ❌ 测试文件（test_*.php）

### 步骤 3: 检查文件大小

```bash
# 查找大文件（>10MB）
dir /s /b | findstr /i ".zip .rar .7z .tar .gz"
```

如果有大文件，考虑：
1. 添加到 .gitignore
2. 使用 Git LFS
3. 提供下载链接

### 步骤 4: 清理临时文件

```bash
# 删除测试文件
del backend\test_*.php
del backend\check_*.php
del backend\fix_*.php

# 删除备份文件
del /s *.bak
del /s *.old
del /s *.tmp
```

### 步骤 5: 验证 .gitignore

```bash
# 测试 .gitignore 是否生效
git check-ignore backend/.env
git check-ignore backend/vendor/
git check-ignore node_modules/
```

如果命令返回文件路径，说明 .gitignore 生效。

## 📝 提交信息规范

使用语义化提交信息：

```bash
# 新功能
git commit -m "feat: 添加支付功能"

# 修复 bug
git commit -m "fix: 修复支付回调问题"

# 文档更新
git commit -m "docs: 更新 README"

# 代码重构
git commit -m "refactor: 重构支付服务"

# 性能优化
git commit -m "perf: 优化数据库查询"

# 测试相关
git commit -m "test: 添加支付单元测试"

# 构建相关
git commit -m "build: 更新依赖版本"

# 配置相关
git commit -m "chore: 更新 .gitignore"
```

## 🚀 上传命令

### 首次上传

```bash
# 1. 初始化仓库
git init

# 2. 添加远程仓库
git remote add origin git@gitee.com:yuchendeloy/tjiajiao91.git

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "feat: 91家教管理系统初始版本

- 管理后台（Vue3 + Element Plus）
- 用户前端（Vue3 + Element Plus）  
- 微信小程序（uni-app）
- 后端API（ThinkPHP 6）
- 支付系统（微信支付）
- 订单管理系统
- 线索管理系统
- 邮件系统"

# 5. 推送到远程
git branch -M main
git push -u origin main
```

### 后续更新

```bash
# 1. 查看更改
git status

# 2. 添加更改
git add .

# 3. 提交
git commit -m "描述你的更改"

# 4. 推送
git push
```

## ⚠️ 常见错误

### 错误 1: 权限被拒绝

```
Permission denied (publickey)
```

**解决方案**：配置 SSH 密钥
```bash
ssh-keygen -t rsa -C "your_email@example.com"
# 将 ~/.ssh/id_rsa.pub 内容添加到 Gitee
```

### 错误 2: 文件过大

```
remote: error: File xxx is 100.00 MB; this exceeds GitHub's file size limit
```

**解决方案**：
1. 添加到 .gitignore
2. 使用 Git LFS
3. 从历史记录中删除

### 错误 3: 已提交敏感文件

**解决方案**：
```bash
# 从 Git 中删除但保留本地文件
git rm --cached backend/.env

# 提交删除操作
git commit -m "chore: 删除敏感配置文件"

# 确保 .gitignore 包含该文件
echo "backend/.env" >> .gitignore
git add .gitignore
git commit -m "chore: 更新 .gitignore"
```

## 📊 上传后验证

1. **访问仓库**：https://gitee.com/yuchendeloy/tjiajiao91

2. **检查文件结构**：
   - 确认所有必要文件都已上传
   - 确认敏感文件未上传

3. **检查 README**：
   - 确认 README.md 正确显示
   - 确认图片和链接正常

4. **克隆测试**：
   ```bash
   cd /tmp
   git clone git@gitee.com:yuchendeloy/tjiajiao91.git
   cd tjiajiao91
   # 检查文件是否完整
   ```

## 🎯 最终检查清单

上传前最后确认：

- [ ] 所有敏感信息已移除
- [ ] .gitignore 文件已配置
- [ ] README.md 内容完整
- [ ] 测试文件已删除
- [ ] 临时文件已清理
- [ ] 提交信息清晰明确
- [ ] SSH 密钥已配置
- [ ] 远程仓库地址正确

**全部确认后，执行上传命令！**

---

## 📞 需要帮助？

如果遇到问题：
1. 查看 Git 文档：https://git-scm.com/doc
2. 查看 Gitee 帮助：https://gitee.com/help
3. 搜索错误信息
4. 联系技术支持

**祝上传顺利！** 🎉
