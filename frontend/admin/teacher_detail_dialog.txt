    <!-- 查看/编辑教师信息弹窗 -->
    <el-dialog 
      v-model="detailVisible" 
      title="编辑教师信息" 
      width="900px"
      :close-on-click-modal="false"
    >
      <el-form 
        ref="detailFormRef" 
        :model="currentTeacher" 
        label-width="120px"
        v-if="currentTeacher"
      >
        <!-- 基本信息 -->
        <el-divider content-position="left">基本信息</el-divider>
        
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="ID">
              <el-input v-model="currentTeacher.id" disabled />
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="姓名">
              <el-input v-model="currentTeacher.name" />
            </el-form-item>
          </el-col>
        </el-row>
        
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="性别">
              <el-radio-group v-model="currentTeacher.gender">
                <el-radio label="男">男</el-radio>
                <el-radio label="女">女</el-radio>
              </el-radio-group>
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="手机号">
              <el-input v-model="currentTeacher.phone" />
            </el-form-item>
          </el-col>
        </el-row>
        
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="微信号">
              <el-input v-model="currentTeacher.wechat_id" />
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="邮箱">
              <el-input v-model="currentTeacher.email" placeholder="未填写" />
            </el-form-item>
          </el-col>
        </el-row>
        
        <!-- 学历信息 -->
        <el-divider content-position="left">学历信息</el-divider>
        
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="学校">
              <el-input v-model="currentTeacher.school" />
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="专业">
              <el-input v-model="currentTeacher.major" />
            </el-form-item>
          </el-col>
        </el-row>
        
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="教师类型">
              <el-select v-model="currentTeacher.teacher_type" placeholder="请选择">
                <el-option label="在读本科生" value="undergraduate"></el-option>
                <el-option label="在读研究生" value="graduate_student"></el-option>
                <el-option label="在读博士生" value="doctoral_student"></el-option>
                <el-option label="毕业生" value="graduated"></el-option>
                <el-option label="专职老师" value="professional"></el-option>
              </el-select>
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="年级/学历">
              <el-input v-model="currentTeacher.grade_level" placeholder="如：大三、研一" />
            </el-form-item>
          </el-col>
        </el-row>
        
        <!-- 个人介绍 -->
        <el-divider content-position="left">个人介绍</el-divider>
        
        <el-form-item label="自我介绍">
          <el-input 
            v-model="currentTeacher.self_introduction" 
            type="textarea" 
            :rows="4"
            maxlength="500"
            show-word-limit
            placeholder="请输入自我介绍"
          />
        </el-form-item>
        
        <el-form-item label="个人优势">
          <el-input 
            v-model="currentTeacher.personal_advantage" 
            type="textarea" 
            :rows="3"
            maxlength="300"
            show-word-limit
            placeholder="请输入个人优势"
          />
        </el-form-item>
        
        <el-form-item label="优势标签">
          <el-select 
            v-model="currentTeacher.advantage_tags" 
            multiple 
            placeholder="请选择优势标签"
            style="width: 100%"
          >
            <el-option label="耐心细致" value="耐心细致"></el-option>
            <el-option label="经验丰富" value="经验丰富"></el-option>
            <el-option label="因材施教" value="因材施教"></el-option>
            <el-option label="提分快" value="提分快"></el-option>
            <el-option label="幽默风趣" value="幽默风趣"></el-option>
            <el-option label="严格要求" value="严格要求"></el-option>
          </el-select>
        </el-form-item>
        
        <!-- 教学信息 -->
        <el-divider content-position="left">教学信息</el-divider>
        
        <el-form-item label="可教科目">
          <el-input 
            v-model="currentTeacher.teaching_subjects" 
            placeholder="如：数学、物理、化学"
          />
        </el-form-item>
        
        <el-form-item label="可教年级">
          <el-input 
            v-model="currentTeacher.teaching_grades" 
            placeholder="如：小学、初中、高中"
          />
        </el-form-item>
        
        <el-form-item label="教学经历">
          <el-input 
            v-model="currentTeacher.teaching_experience" 
            type="textarea" 
            :rows="4"
            maxlength="500"
            show-word-limit
            placeholder="请输入教学经历"
          />
        </el-form-item>
        
        <el-form-item label="教学成果">
          <el-input 
            v-model="currentTeacher.teaching_achievements" 
            type="textarea" 
            :rows="3"
            maxlength="300"
            show-word-limit
            placeholder="请输入教学成果"
          />
        </el-form-item>
        
        <!-- 认证状态 -->
        <el-divider content-position="left">认证状态</el-divider>
        
        <el-form-item label="认证状态">
          <div style="display: flex; gap: 12px; flex-wrap: wrap;">
            <el-tag :type="currentTeacher.real_name_verified ? 'success' : 'info'" size="default">
              实名认证: {{ currentTeacher.real_name_verified ? '已认证' : '未认证' }}
            </el-tag>
            <el-tag :type="currentTeacher.education_verified ? 'success' : 'info'" size="default">
              学历认证: {{ currentTeacher.education_verified ? '已认证' : '未认证' }}
            </el-tag>
            <el-tag :type="currentTeacher.teacher_verified ? 'success' : 'info'" size="default">
              教师认证: {{ currentTeacher.teacher_verified ? '已认证' : '未认证' }}
            </el-tag>
          </div>
        </el-form-item>
        
        <el-form-item label="审核状态">
          <el-tag v-if="currentTeacher.review_status === 'pending'" type="warning">待审核</el-tag>
          <el-tag v-else-if="currentTeacher.review_status === 'approved'" type="success">已通过</el-tag>
          <el-tag v-else-if="currentTeacher.review_status === 'rejected'" type="danger">已拒绝</el-tag>
          <el-tag v-else type="info">未审核</el-tag>
        </el-form-item>
        
        <el-form-item label="置顶显示">
          <el-switch 
            v-model="currentTeacher.is_top" 
            :active-value="1" 
            :inactive-value="0"
            active-text="是"
            inactive-text="否"
          />
        </el-form-item>
        
        <!-- 照片资料 -->
        <el-divider content-position="left">照片资料</el-divider>
        
        <el-form-item label="头像">
          <el-image 
            v-if="currentTeacher.avatar" 
            :src="currentTeacher.avatar" 
            style="width: 120px; height: 120px; border-radius: 8px"
            fit="cover"
            :preview-src-list="[currentTeacher.avatar]"
          />
          <span v-else style="color: #909399;">未上传</span>
        </el-form-item>
        
        <el-form-item label="教学风采">
          <div v-if="currentTeacher.teaching_photos && currentTeacher.teaching_photos.length" style="display: flex; gap: 10px; flex-wrap: wrap;">
            <el-image
              v-for="(photo, index) in currentTeacher.teaching_photos"
              :key="index"
              :src="photo"
              :preview-src-list="currentTeacher.teaching_photos"
              fit="cover"
              style="width: 100px; height: 100px; border-radius: 8px"
            />
          </div>
          <span v-else style="color: #909399;">未上传</span>
        </el-form-item>
        
        <el-form-item label="身份证正面">
          <el-image 
            v-if="currentTeacher.id_card_front" 
            :src="currentTeacher.id_card_front" 
            style="width: 200px; height: 130px; border-radius: 8px"
            fit="cover"
            :preview-src-list="[currentTeacher.id_card_front]"
          />
          <span v-else style="color: #909399;">未上传</span>
        </el-form-item>
        
        <el-form-item label="身份证反面">
          <el-image 
            v-if="currentTeacher.id_card_back" 
            :src="currentTeacher.id_card_back" 
            style="width: 200px; height: 130px; border-radius: 8px"
            fit="cover"
            :preview-src-list="[currentTeacher.id_card_back]"
          />
          <span v-else style="color: #909399;">未上传</span>
        </el-form-item>
        
        <el-form-item label="学历证明">
          <el-image 
            v-if="currentTeacher.education_certificate" 
            :src="currentTeacher.education_certificate" 
            style="width: 200px; height: 130px; border-radius: 8px"
            fit="cover"
            :preview-src-list="[currentTeacher.education_certificate]"
          />
          <span v-else style="color: #909399;">未上传</span>
        </el-form-item>
        
        <el-form-item label="教师资格证">
          <el-image 
            v-if="currentTeacher.teacher_certificate" 
            :src="currentTeacher.teacher_certificate" 
            style="width: 200px; height: 130px; border-radius: 8px"
            fit="cover"
            :preview-src-list="[currentTeacher.teacher_certificate]"
          />
          <span v-else style="color: #909399;">未上传</span>
        </el-form-item>
        
        <!-- 系统信息 -->
        <el-divider content-position="left">系统信息</el-divider>
        
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="注册时间">
              <el-input v-model="currentTeacher.create_time" disabled />
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="更新时间">
              <el-input v-model="currentTeacher.update_time" disabled />
            </el-form-item>
          </el-col>
        </el-row>
      </el-form>
      
      <template #footer>
        <div style="display: flex; justify-content: flex-end; gap: 10px;">
          <el-button @click="handleCancelEdit">取消</el-button>
          <el-button 
            type="primary" 
            @click="handleSaveDetail" 
            :loading="saveLoading"
          >
            保存
          </el-button>
        </div>
      </template>
    </el-dialog>
