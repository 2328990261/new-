# ThinkPHP URL Rewrite Rules for Nginx
# 此文件对应 Nginx 配置中的 #REWRITE-START 部分
# 文件位置：/www/server/panel/vhost/rewrite/t.jiajiao91.com.conf

location / {
    if (!-e $request_filename) {
        rewrite ^(.*)$ /index.php?s=/$1 last;
    }
}

